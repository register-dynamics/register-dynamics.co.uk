---
label: Menu Items
hide_body: true
fields:
- name: links
  type: list
  config:
    use_select: true
    min: 0
    max: 5
    source:
      type: pages
      section: jekyll-pages
  label: Links
pages:
- _data/menu-items.yml
