---
label: Common Grid Options
hide_body: true
fields:
- name: columns
  type: number
  label: Columns
  description: How many columns should be used to display the items.
  default: 1
  required: true
  config:
    min: 1
    max: 4
    step: 1
    required: true
- name: numbered
  type: boolean
  label: Numbered
  description: When switched on, numbers in order will automatically be added to each
    item.

